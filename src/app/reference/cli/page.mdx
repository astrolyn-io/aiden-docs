# Référence CLI

> **Ce que vous allez apprendre**
> - Toutes les commandes AIDEN (workflow, providers, skills, retex)
> - Arguments et options de chaque commande
> - Exemples d'utilisation

## Commandes

### `aiden init`

Initialise AIDEN dans le projet courant.

```bash
aiden init [--enterprise]
```

- `--enterprise` : inclut les templates et règles enterprise

### `aiden flash <description>`

Lance un workflow flash (rapide).

```bash
aiden flash "Fix bug #123" [--dry-run] [--provider <name>]
```

### `aiden standard <description>`

Lance un workflow standard.

```bash
aiden standard "Nouvelle API users" [--dry-run] [--provider <name>]
```

### `aiden enterprise <description>`

Lance un workflow enterprise avec approbation humaine entre chaque phase.

```bash
aiden enterprise "Migration auth" [--dry-run] [--provider <name>] [--auto-approve]
```

Options :
- `--dry-run` : affiche ce qui serait fait sans exécuter
- `--provider <name>` : force un provider spécifique
- `--auto-approve` : bypasse les gates d'approbation humaine (utile en CI/CD)

### `aiden status [feature-slug]`

Affiche le statut des features.

```bash
aiden status                    # Toutes les features
aiden status export-csv         # Une feature spécifique
```

### `aiden resume <feature-slug>`

Reprend une feature en pause ou bloquée.

```bash
aiden resume export-csv
```

### `aiden abort <feature-slug>`

Abandonne une feature (sauvegarde l'état avant suppression).

```bash
aiden abort export-csv
```

### `aiden agent <name> <prompt>`

Invoque un agent directement.

```bash
aiden agent analyst "Analyser le module billing"
aiden agent qa "Reviewer les changements récents"
```

Agents : `analyst`, `planner`, `dev`, `qa`, `docops`.

### `aiden provider`

Gère les providers IA.

```bash
aiden provider status           # État des providers
aiden provider switch gemini    # Changer de provider
aiden provider cost             # Rapport de coûts
```

### `aiden metrics`

Affiche les métriques d'utilisation.

```bash
aiden metrics                   # Affichage texte
aiden metrics --export csv      # Export CSV
```

### `aiden config`

Gère la configuration.

```bash
aiden config show               # Afficher
aiden config set mode flash     # Modifier
```

### `aiden rules`

Gère les règles d'entreprise.

```bash
aiden rules list                # Lister
aiden rules add security.md     # Ajouter
aiden rules remove security     # Supprimer
```

### `aiden skills`

Gère les skills d'extensibilité.

```bash
aiden skills list                # Lister les skills installés
aiden skills init my-skill       # Créer un nouveau skill
aiden skills add /path/to/skill  # Installer depuis un dossier
aiden skills remove my-skill     # Supprimer un skill
aiden skills enable my-skill     # Activer un skill
aiden skills disable my-skill    # Désactiver un skill
```

Les skills sont des packages déclaratifs (YAML + Markdown) dans `.aiden/skills/` qui injectent instructions, règles et hooks dans le pipeline. Voir le [guide skills](/guides/skills) pour plus de détails.

### `aiden retex`

Gère les retours d'expérience (lessons learned).

```bash
aiden retex list                                    # Lister tous les retex
aiden retex add --title "..." --context tag1,tag2 \
  --outcome success --lesson "..." \
  --recommendation "..."                           # Ajouter un retex
aiden retex search <tag>                             # Chercher par tag
aiden retex remove <id>                              # Supprimer un retex
```

Les retex sont stockés dans `.aiden/retex/` en Markdown. Les retex pertinents sont injectés dans les prompts agents sous `--- Lessons Learned ---`.

### `aiden template`

Gère les templates.

```bash
aiden template list             # Lister
aiden template customize spec   # Personnaliser
```

## Slash commands (Claude Code)

AIDEN est aussi disponible en slash commands dans Claude Code :

| Slash command | Équivalent CLI |
|---|---|
| `/aiden-help` | — |
| `/aiden-init` | `aiden init` |
| `/aiden-flash <desc>` | `aiden flash "<desc>"` |
| `/aiden-standard <desc>` | `aiden standard "<desc>"` |
| `/aiden-enterprise <desc>` | `aiden enterprise "<desc>"` |
| `/aiden-status` | `aiden status` |
| `/aiden-resume <slug>` | `aiden resume <slug>` |
| `/aiden-agent <nom> <prompt>` | `aiden agent <nom> "<prompt>"` |
| `/aiden-rules` | `aiden rules list` |

Les deux modes écrivent dans les mêmes fichiers `.aiden/` et sont interchangeables.

## Prochaine étape

Consultez la [référence configuration](/reference/config).
